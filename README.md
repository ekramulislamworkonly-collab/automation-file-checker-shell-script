# automation-file-checker-shell-script
A simple Bash automation script that checks whether a file exists in the current directory.   If the file is not found, the script automatically creates it. This project demonstrates the use of Bash scripting, conditional statements, file handling, and automation concepts in Linux.
